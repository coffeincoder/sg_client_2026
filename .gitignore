
ksb_SG.log
.idea/
test.py
test2.py
/.venv/
/output/
logs/*

# Python
__pycache__/
*.py[cod]
*$py.class

# Виртуальное окружение
venv/
.venv/
env/

# Файл-замок приложения (создаётся при запуске, не код)
app_lock.txt
app.lock

# Логи
*.log

# Сборка PyInstaller (артефакты, генерируются из исходников — не храним в git)
build/
dist/
# (main.spec — конфиг сборки, ОСТАётся в git; ресурсы res/ и INI/ тоже остаются)

# IDE / статический анализ
pyrightconfig.json
.vscode/

# Превью-скрипты и их артефакты (инструменты разработки, не код приложения)
preview_*.py
preview_*.png

# CocoIndex Code (ccc — локальный индекс поиска)
/.cocoindex_code/
